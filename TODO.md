Install and configure mise and aube pkg manager
Create tmux whichkey plugin
Bump all flake versions
Fix rust gui's to open on current workspace by default and not workspace 1
Create some sort of tmux continuity whereby whenever I shutdown/reboot it saves the tmux state and then when rebooting it restores that state
